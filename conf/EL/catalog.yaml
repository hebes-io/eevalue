DATA_DIR: ???
country: EL

sources:
  capacity:
    filepath: ${DATA_DIR}/Capacity/${country}
    filename:
      - COMC_GAS
      - HDR_WAT
      - STUR_LIG
  generation:
    filepath: ${DATA_DIR}/Generation/${country}
    filename:
      - COMC_GAS
      - HDR_WAT
      - STUR_LIG
  res_generation:
    filepath: ${DATA_DIR}/Generation/${country}
    filename: RES
  load: 
    filepath: ${DATA_DIR}/Load/${country}
    filename: load
  reserves: 
    filepath: ${..load.filepath}
    filename: reserves
  imports: 
    filepath: ${DATA_DIR}/NetImports/${country}
    filename: imports
  waters: 
    filepath: ${DATA_DIR}/Hydro/${country}
    filename: waters
  fuel_prices: 
    filepath: ${DATA_DIR}/FuelPrices/${country}
    filename: GAS
  